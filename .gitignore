node_modules/
dist/
*.log
*.tgz

# Per-developer Claude Code state: carries blanket tool permissions and a
# trust flag that must not follow a clone. Shared commands live alongside it
# in .claude/commands/ and are tracked.
.claude/settings.local.json
