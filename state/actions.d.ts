// Types for the `react-timespace/state/actions` subpath (source checkouts only
// — the published tarball exposes `.` and `./theme-config`).

export type { TimeZonesAction } from "../index";

export {
  setState,
  setTimelines,
  addTimeline,
  updateTimeline,
  deleteTimeline,
  setTimeIntervals,
  addTimeInterval,
  updateTimeInterval,
  deleteTimeInterval,
} from "../index";
